/**
 * This module re-exports the `TextEncoderStream` and `TextDecoderStream` APIs
 * from their respective modules, allowing them both to be imported from a
 * single location.
 *
 * @module streams
 */
export * from "./text_encoder_stream.ts";
export * from "./text_decoder_stream.ts";
